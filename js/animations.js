// Contiene codice per alcune animazioni del sito

function animations() {
    const logoFrame = document.querySelector(".logo-frame");
    const logoBack = document.querySelector(".logo-frame-back");
}